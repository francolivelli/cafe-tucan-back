const express = require("express");
const router = express.Router();
const ingredients_controller = require("../controllers/ingredientsController.js");

// CREATE

// READ

// UPDATE

// DELETE

module.exports = router;